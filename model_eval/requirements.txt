# evaluate_models.py'nin temel (RAG'siz) calismasi icin:
requests>=2.31.0

# Sonuc deposu (core/db.py, core/reporting.py) - PostgreSQL'e yazar.
# Mcp_mimarisi ile ayni Postgres sunucusunu paylasir, bkz. ENTEGRASYON.md.
psycopg2-binary>=2.9.0

# RAG / vektor veritabani ozelligi icin (build_vector_db.py, evaluate_models.py --rag).
# --rag bayragini kullanmayanlar bu ikisini kurmak zorunda degildir (evaluate_models.py
# bunlari sadece --rag verildiginde lazy-import eder).
chromadb>=1.5.0
ollama>=0.4.0

# Testleri calistirmak icin (tests/):
pytest>=7.0.0

# Mizan/hesap plani alt kirilim referansi icin (core/mizan.py) - lazy import,
# alt kirilim ozelligini kullanmayanlar bu bagimliliga ihtiyac duymaz.
openpyxl>=3.1.0
