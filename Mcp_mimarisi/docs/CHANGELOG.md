# Changelog

Her commit'e tek satır. Format: `- [TARİH] [özet — ne değişti + neden, tek cümle]`

Amaç: `git log` dökümünden daha okunur bir "proje bugüne kadar ne yaşadı"
özeti — özellikle uzun bir aradan sonra dönen biri (veya bir Claude oturumu)
için.

<!-- Örnek:
- [2026-07-14] feat(auth): JWT yerine session'a geçildi — çoklu-cihaz oturum ihtiyacı doğdu
-->

- [2026-07-22] fix+arch: Çok kullanıcılı MVP mimari denetiminden (GOREV_MIMARI_DUZELTME.md, model_eval ile ortak yapıldı) çıkan 4 madde çözüldü — auth bilinçli kapsam dışı bırakıldı. (1) `fatura_no` mükerrer kayıt race condition: yeni `islenmis_faturalar` claim tablosu, `faturayi_gecmise_kaydet()` artık tek transaction'da `INSERT ... ON CONFLICT DO NOTHING RETURNING` deseniyle atomik; 10 eşzamanlı thread testiyle doğrulandı (1 True, 9 False). (2) Alembic migration sistemi eklendi (`alembic/versions/`, iki migration: mevcut şemanın kaydı + `islenmis_faturalar`); geliştirme DB'sinde `alembic stamp` + `alembic upgrade head` ile gerçekten çalıştırıldı. (3) `GecmisFaturaDeposu` artık `ThreadedConnectionPool` (varsayılan min=2/max=10) kullanıyor, her sorguda yeni bağlantı açmıyor. (4) `api.py`'ye `psycopg2.OperationalError`/`PoolError`/genel `Exception` için `exception_handler`'lar eklendi — DB hatası/pool tükenmesi artık ham hata sızdırmadan `503` + generic mesaj döner, `docker stop`/`start` ile gerçek DB kapatılıp açılarak doğrulandı. Detay: PROJECT.md §3.11, docs/reference/gecmis-fatura-semasi.md, docs/reference/api-semasi.md, docs/how-to/migration-calistirma.md (yeni), docs/how-to/api-calistirma.md.
- [2026-07-17] docs: CLAUDE.md ve PROJECT.md ilk kez dolduruldu — proje amacı, mimari özeti, 3 altın kural ve bilinen zor durumlar (NACE çok-oranlı kodlar, tevkifat eşiği, belirsizlik yönetimi) belgelendi.
- [2026-07-17] arch: Mevzuat MCP'si offline "İzleme & Etiketleme Pipeline" + sorgu-anı "MCP aracı" olarak iki katmana ayrılmasına karar verildi — yüksek hacimde canlı GİB taraması yerine önceden etiketlenmiş, tarih aralıklı bir Mevzuat Kural Deposu sorgulanacak (PROJECT.md §3.6).
- [2026-07-17] arch: Kategori sınıflandırma, geçmiş faturalarla bağımsız bir RAG adımından NACE'ye bağlı kapalı-küme çözümlemeye değiştirildi — geçmiş faturalarda kategori etiketi olmadığı için ilk tasarım hatalıydı, kullanıcı düzeltti (PROJECT.md §3.7).
- [2026-07-17] arch: Mevzuat etiketleme akışı somutlaştırıldı — GİB+mevzuat.gov.tr kaynak, SQLite tabanlı Kural Deposu, LLM-taslak + insan-onay akışı; yargi-mcp incelemesinden çıkan "tutarlı resmi API yok" dersi veri kaynağı kararını şekillendirdi (PROJECT.md §3.6.1, docs/explanation/mevzuat-mcp-mimarisi.md).
- [2026-07-17] arch: Akış sırası tersine çevrildi — doğrulama artık Hesap Planı Eşleme'den ÖNCE çalışıyor (uygunsa TDHP'ye aktarılıyor, değilse hiç girmiyor); kapsam Faz 1 (sadece NACE+Excel kural kontrolü, mevzuat MCP yok) ve Faz 2 (mevzuat MCP + tevkifat/istisna) olarak ikiye bölündü (PROJECT.md §0.1, docs/explanation/akis-sirasi-ve-faz-plani.md).
- [2026-07-17] docs: NACE+KDV excel referans dosyaları (`nace_kdv (1).xlsx`, `nace_kod_degisikligi_2026_03_24.xlsx`) okunup şeması belgelendi — Faz 1 kural kontrolünün veri kaynağı netleşti (docs/reference/nace-kdv-excel-yapisi.md).
- [2026-07-17] feat: UBL-TR e-fatura XML parser'ı yazıldı (`src/efatura_kdv/ubl_parser.py`) — ubls/ altındaki 1828 gerçek fatura üzerinde hatasız test edildi; kritik bulgu: XML'de NACE kodu hiç yok (ayrı kaynaktan gelecek), ve cac:TaxTotal KDV dışı vergi türlerini de (ÖİV, telsiz taksiti) taşıyabiliyor — TaxTypeCode=0015 kontrolü olmadan KDV zannedilebilirdi (docs/reference/ubl-fatura-yapisi.md).
- [2026-07-17] data: `nace_kod_degisikligi_2026_03_24.xlsx`'teki 15 gerçek NACE kod değişikliği (7 birleşme, 6 kod değişikliği, 2 bölünme) için `nace_kdv (1).xlsx`'e ayrı bir `2026_KOD_DEGISIKLIKLERI` sayfası eklendi — ilk denemede eski kodları birincil sayfadan silme yaklaşımı kullanıcı tarafından geri alındı, bunun yerine birincil sayfaya dokunmadan ESKİ KOD→YENİ KOD eşlemesini ayrı sayfada tutma tercih edildi (detay: docs/reference/nace-kdv-excel-yapisi.md).
- [2026-07-17] data: `2026_KOD_DEGISIKLIKLERI` sayfası, sadece 18 satırlık değişiklik özetinden 2026 itibarıyla geçerli **tam** NACE-KDV tablosuna (2138 satır: 2120 değişmeyen + 18 yeni/güncellenmiş kod) genişletildi — `2025_KESIM_BOLUM_GRUP_SINIF` sayfasına hâlâ dokunulmadı, tam doğrulama yapıldı (duplike/eksik/fazla kod yok).
- [2026-07-17] style: `2026_KOD_DEGISIKLIKLERI` sayfasına `2024_KDV_ORANLARI` sayfasındaki sütun-grubu renklendirme deseni (hiyerarşi sütunları pastel tema renkleriyle, KDV oran sütunları renksiz, header kalın+kırmızı) uygulandı — sadece görsel, veri değişmedi.
- [2026-07-17] feat: Faz 1 kural kontrol modülü yazıldı (`src/efatura_kdv/nace_kural_kontrolu.py`) — fatura tarihine göre `2025_KESIM_BOLUM_GRUP_SINIF` (2026-03-24 öncesi) veya `2026_KOD_DEGISIKLIKLERI` (sonrası) sayfasını seçip NACE kodunun izin verdiği oranlarla beyan edilen oranı karşılaştırıyor; 7 senaryoda (tek/çok oranlı NACE, eski/yeni kod geçişi, bulunamayan kod) doğrulandı.
- [2026-07-17] refactor: Kural kontrol modülünden tarih bazlı sayfa seçimi kaldırıldı — kullanıcı kararıyla artık fatura tarihinden bağımsız her zaman güncel `2026_KOD_DEGISIKLIKLERI` sayfası kullanılıyor; `kontrol_et()` artık `fatura_tarihi` parametresi almıyor, 6 senaryoda yeniden doğrulandı.
- [2026-07-17] data: `kdv_oran_referans2.xlsx` (Faz 2 referans şablonu — I/II sayılı liste, tevkifat kodları, NACE_KDV/NACE_ISTISNA) repoya yüklendiği tespit edildi; `NACE_KDV` sayfası (2153 eski satır) `nace_kdv (1).xlsx`'in `2026_KOD_DEGISIKLIKLERI` sayfasından (2138 güncel satır) türetilerek güncellendi — duplike/eksik NACE kodu yok, sadece kaynak veride oranı belirtilmemiş 1 kod (`431200`) boş `aday_oranlar` ile kaldı; diğer 5 sayfaya dokunulmadı; yedek: `kdv_oran_referans2_yedek_2026-07-17.xlsx`.
- [2026-07-20] fix: `nace_kdv (1).xlsx` dosyasının `2026_KOD_DEGISIKLIKLERI` sayfası bir noktada kaybolmuş olduğu tespit edildi (dosya 07-16 tarihli, sayfa fiilen yoktu) — `NaceOranTablosu.__init__` (`src/efatura_kdv/nace_kural_kontrolu.py`) `KeyError` ile çöküyordu, Faz 1 kural kontrolü fiilen çalışmıyordu; sayfa `nace_kod_degisikligi_2026_03_24.xlsx` + ana tablo + istisna sayfası birleştirilerek yeniden oluşturuldu (2138 satır, duplike/eksik yok), `kontrol_et()` gerçek kodlarla test edilip doğrulandı; yedek: `nace_kdv (1)_yedek_2026-07-20.xlsx`.
- [2026-07-20] data: `kdv_oran_referans2.xlsx`'in `NACE_KDV` sayfası, aynı gün onarılan güncel `2026_KOD_DEGISIKLIKLERI` sayfasından yeniden türetildi — eski 2153 satırlık hali senkron değildi; yeni sayfa 2138 satır (1360 KESIN + 778 KALEM_GEREKLI), duplike/eksik yok, `071000`/`089200` istisna notu ve `431200` özel durumu (oransız kaynak veri) korundu; diğer 5 sayfaya dokunulmadı; yedek: `kdv_oran_referans2_yedek_2026-07-20.xlsx`.
- [2026-07-20] feat: Faz 1'in 3. alt-adımı yazıldı — `src/efatura_kdv/kalem_nace_esleme.py`, çoklu-NACE'li satıcılarda kalem (satır) bazında NACE tespiti + KDV oranı kontrolü; satıcının NACE kod(ları) VKN→NACE excel lookup yerine dışarıdan (`SaticiNaceBilgisi`) geliyor (kullanıcı kararı: sistem sadece fatura sahibinin kestiği faturalarda çalışır); çoklu-NACE'de önce sayısal eleme (`NaceCozumTipiTablosu`/`NACE_KDV`), daraltamazsa Ollama `glm-5.2:cloud` ile kapalı-küme LLM seçimi (LLM asla oran üretmiyor); satır bazında KDV yoksa fatura genelindeki tek orana düşülüyor, karışık oranlıysa insan incelemesine düşüyor; 5 gerçek/sentetik senaryoda test edildi (PROJECT.md §0.2 alt-adım 3, docs/explanation/akis-sirasi-ve-faz-plani.md).
- [2026-07-20] feat: Oran uyuşmazlığı gerekçesine istisna farkındalık notu eklendi (`_fatura_istisna_notu()`, `src/efatura_kdv/kalem_nace_esleme.py`) — manuel testte gerçek bir ihracat faturasında (`AKK2025000000071`, istisna kodu 301) %0 oranının NACE listesiyle uyuşmadığı için insan incelemesine düştüğü ama gerekçenin istisnaya hiç değinmediği görüldü; karar hâlâ insan_incelemesi_gerekli kalıyor (istisna doğrulanmıyor, hâlâ Faz 2 kapsamı) ama artık gerekçede "fatura genelinde istisna kodu X mevcut, bu oran farkının sebebi olabilir" notu var; gerçek fatura ile test edildi.
- [2026-07-20] feat: NACE'den bağımsız GENEL istisna kodları (`301`, `302`, `311`, `701` — ihracat/uluslararası taşımacılık/ihraç kayıtlı satış) artık gerçekten doğrulanıyor ve tespit edilirse karar `uygun` oluyor (`_genel_istisna_dogrulamasi()`, `GENEL_ISTISNA_KODLARI`) — kullanıcı "istisna zaten geçerliyse neden hâlâ insan incelemesi gerekiyor" diye sorunca kapsam bilinçli olarak genişletildi; `350`/`351` gibi gerçek istisna maddesi olmayan "diğer" kodlar web araştırmasıyla teyit edilip bilinçli olarak dışarıda bırakıldı (hâlâ sadece bilgi notu + insan_incelemesi_gerekli). Gerçek 301-kodlu ihracat faturası (→ uygun) ve 351-kodlu regresyon faturası (→ hâlâ insan_incelemesi_gerekli) ile test edildi.
- [2026-07-20] refactor: kalem_nace_esleme.py mimarisi kullanıcı kararıyla basitleştirildi — kalem METNİ artık hiç okunmuyor, LLM (Ollama/glm-5.2:cloud) tamamen kaldırıldı (`kalem_nace_esle()`, `_llm_nace_sec()`, `NaceCozumTipiTablosu`, `EslemeGuveni` silindi); yeni mantık satıcının TÜM NACE kod(lar)ının izin verdiği oranları tek bir havuzda birleştirip (`_izin_verilen_oranlar_havuzu()`) kalemin oranı bu havuzda mı diye bakıyor — kalem içeriğine hiç bakılmadan. `satir_bazli_kontrol_et()` artık sadece `nace_kdv (1).xlsx`/`NaceOranTablosu` kullanıyor, `kdv_oran_referans2.xlsx`'e ihtiyaç kalmadı. İstisna kontrolü ve genel-toplam fallback değişmeden korundu. `test/test_kalem_nace.py` ve `test/web_arayuz.py` yeni API'ye güncellendi, artık kullanılmayan `test/llm_gerekce_tanisi.py` silindi; 6 senaryoda test edildi.
- [2026-07-20] feat: "uygun" gerekçesine hangi NACE kodunun beyan edilen oranı desteklediği eklendi (`_nace_gerekce_metni()`, `_izin_verilen_oranlar_havuzu()` artık her NACE'nin kendi oranlarını da ayrıca döndürüyor) — önceden gerekçe sadece birleşik havuzu gösteriyordu (ör. "havuzda [1,10,20]"), artık "%20 -> NACE ['532009']" gibi tam olarak hangi NACE(ler)in bu oranı meşru kıldığını gösteriyor; birden fazla NACE aynı oranı destekliyorsa hepsi listeleniyor. Gerçek çoklu-NACE senaryolarıyla test edildi.
- [2026-07-21] feat: Geçmiş fatura kalemleriyle çapraz kontrol katmanı eklendi — kullanıcı `ubls/` klasöründeki geçmiş faturaların "bu kalem daha önce bu oranla mı kesilmiş" diye kontrol edilmesini istedi; bu istek ilk bakışta PROJECT.md §3.7'nin "geçmiş fatura verisi sınıflandırmada girdi olarak kullanılmaz" kararıyla çelişiyor gibi göründüğü için önce kapsam netleştirildi — kullanıcı onayladı ki bu KARAR ÜRETMEZ, sadece bilgi/uyarı katmanıdır (NACE kural kontrolünün ürettiği karar hiç değişmez), bu yüzden §3.7'nin yasakladığı "geçmişten öğrenip karar üretme" riskini taşımaz; PROJECT.md'ye §3.9 olarak bu ayrım açıkça belgelendi. Sadece OUTBOX faturalar (bizim kestiğimiz, 380/1828 dosya, hepsi tek satıcı VKN'sinde doğrulandı) kaydedildi — inbox faturalar bizim doğruluğumuzu yansıtmadığı için hariç tutuldu. Yeni tablo `gecmis_fatura_kalemleri` (kalem bazında, `satici_vkn + kalem_adi_normalize` üzerinde indeksli), migrasyon `scripts/gecmis_faturalari_yukle.py` (377/380 outbox işlendi, 939 kalem-oran satırı — 3 fatura ne kalemde ne genelde KDV oranı taşımadığı için atlandı, gerçek veri eksikliği), kod `src/efatura_kdv/gecmis_kontrol.py` (normalize edilmiş TAM string eşleşmesi, bulanık eşleşme kullanılmadı — kullanıcı kararı), yeni endpoint `POST /fatura/gecmis-kontrol` (ayrı, ana `/fatura/kontrol-et` akışına karışmaz — kullanıcı kararı). Gerçek PostgreSQL ile 3 senaryoda test edildi: geçmişle uyumlu (`DIN 15412 RS 5 P TRAVERS` → %20, 21 kez), geçmişle çelişen (aynı kalem %10 beyan edilince "mevzuat değişmiş olabilir" uyarısı), hiç görülmemiş kalem; ayrıca `/fatura/kontrol-et`'in kararının bu yeni katmandan etkilenmediği doğrulandı. Şema: `docs/reference/gecmis-fatura-semasi.md`, kurulum: `docs/how-to/api-calistirma.md`, karar: PROJECT.md §3.9.
- [2026-07-22] arch: Ana projeye (TDHP tahmin pipeline'ı) entegrasyon kararı belgelendi — kullanıcı "bu klasör yapısı benim ana projemin bir dalı, ana kısma nasıl ulaşırız" diye sordu; entegrasyon HTTP API üzerinden ayrık olacak (monorepo değil), Mcp_mimarisi'nin `POST /fatura/kontrol-et`'i TDHP pipeline'ının XML parse adımından ÖNCE "Adım 0" olarak çalışacak — `genel_karar="uygun"` değilse TDHP tahminine hiç girilmeyecek. **Düzeltme (aynı gün):** İlk turda yanlışlıkla `~/Desktop/AIData/` (eski/yedek klasör, İngilizce "I") güncellenmişti; kullanıcı düzeltti — gerçek aktif proje `~/Desktop/AıData2/model_eval/`dir (Türkçe "ı", aynı üst klasör, gerçek RAG+çoklu-model TDHP tahmin pipeline'ı, `core/parsing.py`/`RESULTS.md`/`yeni_faturalar_tdhp.md`). `AIData/AGENTS.md` ve `AIData/mimari.md`'deki hatalı eklemeler geri alındı; doğru karar `model_eval/entegrasyon.md` (yeni dosya) ve bu reponun `PROJECT.md` §3.10'una yazıldı. Henüz kod yazılmadı — bu bir sözleşme/karar kaydı.
- [2026-07-22] fix: İstisna kaynaklı kalemler geçmiş fatura veritabanına artık kaydediliyor — kullanıcı `AKK2025000000003-988e5bd8-cc92-468f-8058-816d8a9e89a5-outbox.xml` faturasının gerçek bir ihracat istisnası (kod 301) taşımasına rağmen geçmiş kontrolünde hiç görünmediğini bildirdi; kök neden: kalemin oranı (`Percent`) hiç yazılmamış (istisna kapsamında normal) ve `FaturaKalemi.kdv_oranlari` bu durumda boş liste döndürdüğü için `scripts/gecmis_faturalari_yukle.py` bu kalemleri sessizce atlıyordu (3 fatura "veri eksikliği" sanılıyordu, aslında istisna kaynaklıydı). `src/efatura_kdv/kalem_nace_esleme.py`'deki `_kalem_istisna_kodlari()` public yapıldı (`kalem_istisna_kodlari`, `_`-prefiksi kaldırıldı) ve `gecmis_faturalari_yukle.py` artık oran yoksa istisna kodu arayıp `oran=0.0` + yeni `istisna_kodu` sütunuyla kaydediyor; `gecmis_fatura_kalemleri` tablosuna `ALTER TABLE ... ADD COLUMN IF NOT EXISTS istisna_kodu TEXT` ile sütun eklendi; `gecmis_kontrol.py`'deki sorgu `GROUP BY oran, istisna_kodu` oldu, `GecmisOranOzeti`ye `istisna_kodu` alanı eklendi, `bilgi_notu` artık "%0, istisna kodu 301" gibi ayrım gösteriyor; API/web arayüzünün otomatik kayıt akışı (`faturayi_gecmise_kaydet`) için de aynı mantık yeni ortak fonksiyon `fatura_kalemlerini_kayit_icin_hazirla()`'ya taşındı (kod tekrarı yerine tek kaynak, `api.py` ve `test/web_arayuz.py` ikisi de bunu kullanıyor). Gerçek Postgres ile doğrulandı: migrasyon artık 380/380 outbox faturasını işliyor (önceden 377/380), 961 satır (939'dan +22); `AKK2025000000003`'ün 8 kalemi artık doğru kaydediliyor; `POST /fatura/coklu-kontrol` ile aynı fatura gerçek `curl` isteğiyle gönderildi, ilk gönderim → kaydedildi, tekrar gönderim → yinelenen engellendi (davranış korundu). Şema: docs/reference/gecmis-fatura-semasi.md.
- [2026-07-21] feat: web_arayuz.py (test/web_arayuz.py) 3 sekmeli sonuç görünümüne kavuştu — kullanıcı çoklu fatura yükleme sonuçlarında VKN uyuşmayan (bize ait olmayan) faturaların ayrı bir sekmede toplanmasını istedi ("bu faturayı biz kesmedik, fatura bize gelsin diye uyarsın"); sekmeler: Başarılı / VKN Uyuşmazlığı / Diğer Hatalar (bozuk XML vb.). Bunu ayırt etmek için `src/efatura_kdv/kalem_nace_esleme.py`'ye yeni bir exception sınıfı `SaticiVknUyusmazligiHatasi` eklendi (ValueError'dan türetildi — mevcut `except ValueError` blokları hâlâ yakalıyor, geriye dönük uyumlu), `satir_bazli_kontrol_et()`'teki VKN güvenlik kontrolü artık bu spesifik hatayı fırlatıyor; web_arayuz.py `except SaticiVknUyusmazligiHatasi` bloğunu genel `except Exception`'dan önce yakalayıp ayrı listeye topluyor, saf CSS+JS ile (harici kütüphane yok) sekme geçişi eklendi. Gerçek 3 senaryoyla (outbox fatura+doğru VKN → Başarılı, inbox fatura+yanlış VKN iddiası → VKN Uyuşmazlığı, bozuk XML → Diğer Hatalar) `curl` ile test edildi, üç sekme de doğru sayıda sonuç gösterdi.
- [2026-07-21] feat+fix: MVP için çoklu fatura yükleme + otomatik geçmiş kaydı eklendi (`POST /fatura/coklu-kontrol` API endpoint'i, `test/web_arayuz.py` çoklu-dosya desteği) — kullanıcı sisteme "gerçek ilk teste sokma" hazırlığında aynı satıcı+NACE ile birden fazla fatura toplu yüklenebilsin istedi; başarıyla kontrol edilen (VKN uyuşmazlığı olmayan) her fatura otomatik `gecmis_fatura_kalemleri`'ne yazılıyor (`faturayi_gecmise_kaydet()`, yinelenen fatura_no tekrar yazılmıyor); kapsam netleştirmesi sonunda kullanıcı "sadece bizim kestiğimiz faturaları incelemeli" dedi, inbox desteği eklenmedi. **Kritik hata bulundu ve aynı gün düzeltildi:** web arayüzünün ilk sürümü kullanıcının kendi VKN'sini hiç sormuyordu, faturanın kendi satıcı VKN'sini kullanıyordu — bu yüzden bir Turkcell (inbox) faturası yanlışlıkla geçmiş veritabanına kaydedildi; tespit edilip hatalı kayıt silindi, form artık "Sizin şirketinizin VKN'si" alanını zorunlu istiyor ve gerçek satıcı VKN'siyle karşılaştırıyor (API tarafı baştan doğruydu). Gerçek Postgres ile test edildi: yeni fatura→kaydedildi, tekrar gönderim→kaydedilmedi (çiftlenme yok), inbox→reddedildi, karışık istek (1 geçerli+1 bozuk XML)→geçerli işlendi. Detay: PROJECT.md §3.9.1, docs/reference/api-semasi.md, docs/reference/gecmis-fatura-semasi.md.
- [2026-07-21] test: Kullanıcı Docker'ı kurup Postgres Desktop'ı unpause ettikten sonra PostgreSQL + API katmanı gerçek DB bağlantısıyla uçtan uca doğrulandı (önceki mock testlerin yerini aldı) — bu makinede zaten iki yerel PostgreSQL kurulumu (16/17) 5432/5433 portlarını tuttuğu için Docker container'ı port 5434'te açıldı; `scripts/excel_to_postgres.py` 2138 NACE kodunu doğru yazdı; `NaceOranTablosu`/`kontrol_et()` gerçek satırlarla (532009→uygun, bilinmeyen kod→insan_incelemesi_gerekli) doğrulandı; API gerçek `uvicorn` süreciyle başlatılıp `curl` ile test edildi: health check, gerçek fatura → doğru karar, 5 eşzamanlı istek → tutarlı sonuç, bozuk XML → 400, VKN uyuşmazlığı → 400; docs/how-to/postgres-kurulum.md, docs/how-to/api-calistirma.md, docs/reference/api-semasi.md, PROJECT.md §3.6.1/§3.8'deki "henüz test edilmedi" notları güncellendi.
- [2026-07-21] feat: Faz 1 doğrulama katmanı bir HTTP API'sine (FastAPI, `src/efatura_kdv/api.py`) kavuştu — kullanıcı "tüm altyapıyı çok kullanıcılı hale getir" isteğinin devamı; tek endpoint `POST /fatura/kontrol-et` (ham UBL-TR XML + satıcı VKN + NACE kodları → kalem bazlı karar) + `GET /saglik`; `NaceOranTablosu` uygulama başlarken bir kez PostgreSQL'den yüklenip tüm istekler arasında bellek-içi paylaşılıyor (her istek ayrı DB bağlantısı açmıyor), istekler arası paylaşılan mutable state yok; `ubl_parser.py`'ye `parse_ubl_invoice_from_string()` eklendi (ortak ayrıştırma mantığı `_parse_root()`'a çıkarıldı, dosya/string ikisi de onu kullanıyor) — kod tekrarını önlemek için refactor edildi; auth ve yük testi bilinçli olarak kapsam dışı bırakıldı (kullanıcı sadece HTTP API katmanı istedi); Python 3.9 uyumluluğu için Pydantic modellerinde `X | None` yerine `Optional[X]` kullanıldı (bu ortamda `X | None` runtime'da pydantic ile çözülemiyor). Mock PostgreSQL bağlantısıyla (`unittest.mock`, gerçek Docker/Postgres bu ortamda kurulu olmadığı için) uçtan uca test edildi: health check, gerçek fatura ile doğru "uygun" kararı, bozuk XML → 400, satıcı VKN uyuşmazlığı → 400; gerçek PostgreSQL ile henüz test edilmedi. Kurulum: `docs/how-to/api-calistirma.md`, şema: `docs/reference/api-semasi.md`, karar: PROJECT.md §3.8.
- [2026-07-21] arch: Faz 1'in NACE+oran veri kaynağı excel'den (`nace_kdv (1).xlsx`) PostgreSQL'e taşındı — kullanıcı çoklu-kullanıcı/eşzamanlı erişim ihtiyacını belirtince, PROJECT.md §3.6.1'deki "Mevzuat Kural Deposu SQLite ile başlar" kararı PostgreSQL olarak güncellendi ve bu karar Faz 1'in NACE tablosuna erken uygulandı; `NaceOranTablosu` (`src/efatura_kdv/nace_kural_kontrolu.py`) artık excel açmıyor, `DATABASE_URL` env var ile `nace_oranlari` tablosunu sorguluyor, dış arayüzü (`izin_verilen_oranlar()`) değişmedi (çağıran kod `kalem_nace_esleme.py`/testler etkilenmedi); tek seferlik migrasyon betiği `scripts/excel_to_postgres.py` eklendi; kurulum adımları `docs/how-to/postgres-kurulum.md`'de, şema `docs/reference/nace-kdv-excel-yapisi.md`'de belgelendi; Docker/Postgres bu ortamda kurulu olmadığı için gerçek DB bağlantısıyla uçtan uca test YAPILAMADI — sadece DATABASE_URL yokken beklenen hata davranışı doğrulandı, kullanıcının ortamı kurduktan sonra gerçek test gerekiyor.
- [2026-07-20] data+feat: `Istisna_Kodlari_GIB.xlsx` (GİB e-Belge Uygulamaları - UBL-TR Kod Listeleri Kılavuzu, V1.42, Mart 2026) repoya eklendi ve `GENEL_ISTISNA_KODLARI` (`src/efatura_kdv/kalem_nace_esleme.py`) bu kaynağın 7 kategorisinin tamamından (Kısmi/Tam İstisna, İhraç Kayıtlı Satışlar, Özel Matrah, ÖTV İstisna, Konaklama Vergisi İstisna, Diğer İşlem Türü) türetilerek 4 koddan 107 koda genişletildi — açıklama metninde "İstisna Olmayan"/"Diğerleri" geçen 4 dolgu kod (151, 250, 350, 351) bilinçli olarak hariç tutuldu; bu kaynak ayrıca önceki web araştırmasında şüpheli görünen 701 kodunun (İhraç Kayıtlı Satışlar kategorisi) gerçekten geçerli olduğunu doğruladı. Şema belgelendi: docs/reference/istisna-kodlari-gib-yapisi.md. Gerçek (301→uygun, 351→hâlâ insan incelemesi) ve sentetik (701, 235→uygun) senaryolarla test edildi.
